# Sidecar-only portal backend selection. Do not apply to the standard portal.
[Service]
Environment=XDG_CURRENT_DESKTOP=credentials-sidecar
Environment=XDG_DESKTOP_PORTAL_ENABLE_EXPERIMENTAL=credential
